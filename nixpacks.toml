[phases.setup]
# nodejs_20 includes npm, ffmpeg-full includes ffprobe for video analysis
nixPkgs = ["nodejs_20", "ffmpeg-full"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && node server.js"
